/**
 * Generalized package containing the interface to the network (over serial). While {@link net.Network} contains the functions which can be called to handle connections and send data, it needs an implementation of {@link net.Network_iface} to react to data arriving from the serial interface and writing information to the log.
 * 
 *  @author Raphael Blatter (raphael@blatter.sg)
 *  @author heavily using code examples from the RXTX-website (rxtx.qbang.org)
 */

package net;

